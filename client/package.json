{
    "main": "./public/index.html",
    "devDependencies": {
        "@types/classnames": "2.2.10",
        "@types/node": "latest",
        "@types/react": "16.9.23",
        "@types/react-dom": "16.9.5",
        "http-server": "latest",
        "json-schema-to-typescript": "latest",
        "parcel-bundler": "latest",
        "prettier": "latest",
        "typescript": "latest"
    },
    "dependencies": {
        "@babel/polyfill": "7.8.7",
        "axios": "0.19.2",
        "brace": "0.11.1",
        "classnames": "2.2.6",
        "emotion": "10.0.27",
        "react": "16.13.0",
        "react-contexify": "4.1.1",
        "react-dom": "16.13.0"
    },
    "scripts": {
        "format": "shopt -s extglob globstar && prettier --config ./.prettierrc.json -c --write ./src/**/*.ts* && shopt -u extglob globstar",
        "schema": "mkdir -p ./src/schema && json2ts -i ../schemas/schema.json -o ./src/schema/schema.ts",
        "build": "parcel build --no-cache --no-source-maps -d ./dist/build -- \"$npm_package_main\"",
        "start": "{ [ \"$PORT\" ] || PORT=1234; } && { [ \"$API\" ] || { echo 'API not defined!' && exit 1; }; } && echo \"PORT: $PORT, API: $API\" && npm run build && http-server -p \"$PORT\" -- ./dist/build",
        "start:dev": "{ [ \"$PORT\" ] || PORT=1234; } && { [ \"$API\" ] || { echo 'API not defined!' && exit 1; }; } && echo \"PORT: $PORT, API: $API\" && parcel serve --no-cache -d ./dist/ -p \"$PORT\" -- \"$npm_package_main\""
    }
}
