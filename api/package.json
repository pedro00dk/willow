{
    "main": "./src/index.ts",
    "type": "module",
    "devDependencies": {
        "firebase-tools": "8.2.0"
    },
    "scripts": {
        "login": "firebase login --reauth",
        "init": "firebase init",
        "start": "firebase emulators:start",
        "deploy": "firebase deploy"
    }
}
